AGENTIC-VT 1.0

[metadata]
title: Claude Code's system tools are SO BLOATED
channel: @octopusinc_
duration: 01:38
source: https://www.youtube.com/watch?v=oLx4yCbeklQ
upload_date: 20260716
transcript_source: captions+frames
route: visual

---

[00:00] [scene:intro]
VISUAL: The speaker introduces the problem of bloat in Claude Code's system prompt. A webpage from AIhero.dev is shown, detailing how to find offenders with a logging proxy and displaying a table with token usage for "agent-proxy", "Workflow", "DesignSync", and "Monitor". The webpage from AIhero.dev is still visible, with "Workflow" highlighted in the token usage table.
AUDIO: 'Most harnesses, but especially Claude Code, ship with a ton of bloat in the system prompt. There is very likely thousands and thousands of tokens of stuff you\'re not using in your system prompt. So, when I did mine, I found that I had an whole stuff with workflow,'

[00:13] [scene:ui]
VISUAL: The webpage from AIhero.dev is still visible, with "DesignSync" and "Monitor" highlighted in the token usage table. The speaker explains the token cost of unused tools. A code snippet from a `settings.json` file is displayed, showing a "permissions" object with various tools listed, and several `disable` flags set to `true`.
AUDIO: 'with design sync, with monitor, that I simply wasn\'t using. And that is about, you know, 8,000, 10,000 tokens per request. Fortunately, Claude Code allows you to customize this stuff, so you can actually put this in your global'

[00:26] [scene:talking_head]
VISUAL: The speaker explains that the `settings.json` file allows disabling useless features. The `settings.json` file is still visible, with "EnterPlanMode" and "ExitPlanMode" highlighted.
AUDIO: 'settings.json file, and you can disable all sorts of useless stuff. So, I didn\'t want it to control when I entered and exited plan mode. So, I just disabled those tools, and the tool definitions get removed from the system prompt. I'

[00:39] [scene:talking_head]
VISUAL: The speaker expresses dislike for the "ask user question" tool. The `settings.json` file is still visible, with "AskUserQuestion" highlighted. The `settings.json` file is still visible, with "CronCreate", "CronDelete", and "CronList" highlighted.
AUDIO: 'personally really hate the ask user question tool, so I never use it, or I never want to see it, and so I just removed the tool definition from Claude system prompt. Equally, I don\'t want it to schedule crons for me, so I just deleted those as well. I don\'t use the'

[00:52] [scene:talking_head]
VISUAL: The speaker mentions disabling the custom code review skill. The `settings.json` file is still visible, with "disableDynamicWorkflows" highlighted. The `settings.json` file is still visible, with "disableRemoteControl" highlighted.
AUDIO: 'custom code review skill that ships with it, so I disabled the bundled skills. I disabled everything to do with dynamic workflows. I don\'t use those either. I even disabled remote control, because there was a lot actually in the system'

[01:04] [scene:ui]
VISUAL: The `settings.json` file is still visible, with "disableClaudeAIConnectors" highlighted. The speaker summarizes the reduction in system prompt size.
AUDIO: 'prompt for that. Disabled Claude AI connectors, which were burning a ton of tokens I didn\'t even realize. And I disabled the artifacts feature. All of that means that I dropped my initial starting system prompt, or the system tools, from 25K down to around 8K. I put'

[01:18] [scene:talking_head]
VISUAL: The speaker states the token reduction from 25K to 8K. The `settings.json` file is still visible, showing the full configuration for disabling various tools and features. The speaker explains the benefit of tuning settings for the system prompt.
AUDIO: 'this in an article so that you can use a proxy to see what you\'re actually shipping, which is really, really useful. Then, you can tune the settings to choose just the bits of the system prompt that you need. The less you send'

[01:31] [scene:talking_head]
VISUAL: The speaker concludes that less data sent improves agent output.
AUDIO: 'over the wire, the better your outputs are going to be, because the agent has less stuff distracting it.'
